[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "benimkutuphanem"
version = "0.4.0"
description = "Kendi Python kutuphanem"
authors = [
    {name = "Sen"}
]